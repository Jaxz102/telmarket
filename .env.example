TELEGRAM_TOKEN=123456:ABC-your-bot-token
# Numeric channel id (starts with -100). Find it with: python bot.py --discover
TELEGRAM_CHAT_ID=-1001234567890
# Optional overrides
# INTERVAL_HOURS=6
# LOOKBACK_HOURS=7   # defaults to INTERVAL_HOURS + 1
